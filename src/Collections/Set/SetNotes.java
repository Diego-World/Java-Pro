package Collections.Set;

public class SetNotes {

    /*
     * SET:
     * - Pode ser heterogêneo e homogêneo
     * - Não aceita objetos duplicados
     * - Pode ser ordenado, mas não é por padrão
     * - Não é indexado
     */

    /*
     *   De maneira resumida, um Set representa um conjunto de valores, o qual não possui elementos duplicados.
     *   Dois objetos Set serão idênticos caso contenham o mesmo número de elementos.
     *   A linguagem Java disponibiliza três tipos de implementação Set diferentes, o ConjuntoHashSet, o TreeSet e o LinkedHashSet.
     */

    /*  É importante notar que TreeSet, HashSet e LinkedHashSet implementam a interface “Set”, ou seja,
    *   temos os mesmos métodos para as 3 estruturas, o que difere cada uma é a forma com que é implementado o algoritmo,
    *   por exemplo, na HashSet já falamos que ela usa HashTable em sua implementação,
    *   que por sinal é muito rápido mas não garante a ordenação dos seus elementos.
    */

    /*
    * Muito importante o uso do Equals e HasCode para operações de remoção, comparação e etc...
    */

}
